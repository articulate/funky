{
  "name": "@articulate/funky",
  "version": "0.0.1",
  "description": "Functional helper library",
  "main": "index.js",
  "repository": "git@github.com:articulate/funky.git",
  "author": "articulate",
  "license": "UNLICENSED",
  "scripts": {
    "build": "buble -i src -o lib",
    "clean": "rm -rf lib && mkdir -p lib",
    "lint": "eslint src test",
    "postversion": "git push --tags origin master",
    "prebuild": "yarn run clean",
    "prepublish": "git checkout master && yarn run build",
    "pretest": "yarn run build",
    "preversion": "git checkout master",
    "test": "mocha --reporter dot"
  },
  "dependencies": {
    "joi": "^10.6.0",
    "ramda": "^0.24.1"
  },
  "devDependencies": {
    "buble": "^0.15.2",
    "chai": "^4.1.1",
    "eslint": "^4.3.0",
    "mocha": "^3.5.0",
    "prop-factory": "^1.0.0"
  }
}
