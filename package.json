{
  "name": "@articulate/funky",
  "version": "0.0.0",
  "description": "Functional helper library",
  "main": "index.js",
  "repository": "git@github.com:articulate/funky.git",
  "author": "articulate",
  "license": "UNLICENSED",
  "scripts": {
    "build": "buble -i src -o lib",
    "clean": "rm -rf lib",
    "lint": "eslint src test",
    "prebuild": "yarn run clean && mkdir -p lib",
    "pretest": "yarn run build",
    "test": "mocha --reporter dot"
  },
  "dependencies": {
    "joi": "^10.6.0",
    "ramda": "^0.24.1"
  },
  "devDependencies": {
    "buble": "^0.15.2",
    "chai": "^4.1.1",
    "eslint": "^4.3.0",
    "mocha": "^3.5.0",
    "prop-factory": "^1.0.0"
  }
}
